# Core terminal simulator requirements for 0Term

# GUI
tkinter          # comes preinstalled with most Python builds

# Terminal & color handling
colorama>=0.4.6   # Safe ANSI color handling for Windows/Linux
pyte>=0.8.1        # (Optional) For future TUI/ANSI parsing & emulation

# Utilities
platformdirs>=3.10.0  # For managing user paths/config folders (optional)
